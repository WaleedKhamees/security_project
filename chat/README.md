# Part 1: Secure Chat

Pip Requirements:

```bash
pip install pycryptodome
pip install sympy
pip install cryptography
```

start the server then the client: 

```bash 
python server.py
python client.py
```
